{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Capability for Entropy's main window. Grants the native dialog (folder picker), the specific fs read commands the local arcdps log auto-import feature needs, and the updater/process commands the auto-update-on-launch feature needs (check for a new release, download+install it, and restart into it).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "fs:allow-read-dir",
    "fs:allow-read-file",
    "fs:allow-stat",
    "updater:default",
    "process:default"
  ]
}
