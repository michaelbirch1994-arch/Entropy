name: Conditions legacy normalization script runner

on:
  push:
    branches:
      - feature/conditions-legacy-normalization

permissions:
  contents: write

jobs:
  edit:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/conditions-legacy-normalization
          fetch-depth: 0

      - name: Apply edit
        run: python scripts/apply-conditions-legacy-normalization.py

      - name: Commit result
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/workflows/conditions-normalization-script-runner.yml scripts/apply-conditions-legacy-normalization.py
          git add src/views/ConditionsView.tsx
          git diff --cached --check
          git commit -m "Normalize legacy Conditions rows"
          git push origin HEAD:feature/conditions-legacy-normalization
