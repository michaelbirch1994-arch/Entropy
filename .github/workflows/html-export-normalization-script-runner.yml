name: HTML export legacy normalization script runner

on:
  push:
    branches:
      - feature/html-export-legacy-normalization

permissions:
  contents: write

jobs:
  edit:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/html-export-legacy-normalization
          fetch-depth: 0

      - name: Apply edit
        run: python scripts/apply-html-export-legacy-normalization.py

      - name: Commit result
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/workflows/html-export-normalization-script-runner.yml scripts/apply-html-export-legacy-normalization.py
          git add src/lib/exportReportHtml.ts
          git diff --cached --check
          git commit -m "Normalize legacy HTML export player rows"
          git push origin HEAD:feature/html-export-legacy-normalization
