name: Squad pressure normalization script runner

on:
  push:
    branches:
      - feature/squad-overview-legacy-pressure

permissions:
  contents: write

jobs:
  edit:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/squad-overview-legacy-pressure
          fetch-depth: 0
      - name: Apply edit
        run: python scripts/apply-squad-pressure-normalization.py
      - name: Commit result
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm scripts/apply-squad-pressure-normalization.py .github/workflows/squad-pressure-script-runner.yml
          git add src/views/SquadStatsView.tsx
          git diff --cached --check
          git commit -m "Normalize legacy Top DPS and Kill Pressure rows"
          git push origin HEAD:feature/squad-overview-legacy-pressure
