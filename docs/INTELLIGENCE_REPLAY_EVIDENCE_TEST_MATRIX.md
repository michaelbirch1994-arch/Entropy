# Replay evidence test matrix

The replay evidence slice is validated against:

- exact event timestamp interpolation;
- squad alive/down counts;
- tracked enemy alive/down counts;
- commander presence;
- average squad distance to commander;
- linked player account/name matching;
- fight isolation;
- out-of-range timestamp clamping.
